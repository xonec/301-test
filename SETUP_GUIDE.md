# 微信小程序项目配置指南

## ⚠️ "请先设置小程序" 错误说明

当你在微信开发者工具中看到这个提示时，说明 `project.config.json` 中的 AppID 还没有设置为你自己的小程序 AppID。

## 🔧 快速修复步骤

### 第 1 步：获取你的 AppID

1. **登录** [微信公众平台](https://mp.weixin.qq.com)
2. 选择你的小程序（如果还没有，需要先注册）
3. 进入 **开发** → **开发管理** → **开发设置**
4. 在"开发者ID"部分找到 **AppID**

示例：`wxa640c845f511b53c`

### 第 2 步：更新项目配置

编辑项目根目录的 `project.config.json` 文件，找到这一行：

```json
"appid": "wxa640c845f511b53c",
```

替换为你的 AppID：

```json
"appid": "your_appid_here",
```

例如：
```json
"appid": "wxe1234567890abcdef",
```

### 第 3 步：重启微信开发者工具

1. 关闭微信开发者工具
2. 重新打开项目
3. 工具会自动识别新的 AppID
4. 错误提示应该消失

## 📋 完整的项目配置

### project.config.json 关键字段

```json
{
  "description": "项目配置文件",
  "appid": "your_appid_here",           // ← 改这里！
  "projectname": "YourProjectName",     // 项目名称
  "compileType": "miniprogram",         // 项目类型：小程序
  "libVersion": "3.11.2",               // 基础库版本
  "setting": {
    "nodeModules": true,                // 启用 npm 模块
    "useIsolateContext": true,          // 隔离作用域
    "skylineRenderEnable": true,        // 启用 Skyline 渲染引擎
    // ... 其他配置
  }
}
```

## 🔑 获取 AppID 的详细步骤

### 方式 1：通过微信公众平台网页

1. 访问 https://mp.weixin.qq.com
2. 用管理员账号登录
3. 左侧菜单 → **开发管理**
4. 找到 **开发者ID** 部分
5. 复制 **AppID** 的值

### 方式 2：通过微信开发者工具

如果你已经在微信开发者工具中登录：

1. 工具顶部菜单 → **项目** → **项目设置**
2. **本地设置** 选项卡
3. 查看 **AppID** 字段

## ✅ 验证配置是否成功

修改完 `project.config.json` 后，检查：

1. ✅ 微信开发者工具没有提示错误
2. ✅ 工具顶部显示项目名称和 AppID
3. ✅ 可以正常编译和运行
4. ✅ 可以预览小程序

## ⚙️ 其他重要配置

### app.json 配置

```json
{
  "pages": [
    "pages/login/login",              // 登录页面
    "pages/statistics-outer/statistics-outer",
    "pages/statistics-inner/statistics-inner",
    "pages/statistics-std/statistics-std",
    "pages/user/user"                 // 用户中心页面
  ],
  "window": {
    "navigationBarTitleText": "TDesign",
    "navigationStyle": "custom"
  },
  "tabBar": {                          // 底部导航栏
    "color": "#6B7280",
    "selectedColor": "#2563EB",
    "list": [
      { "pagePath": "pages/statistics-outer/statistics-outer", "text": "外包" },
      { "pagePath": "pages/statistics-inner/statistics-inner", "text": "内包" },
      { "pagePath": "pages/statistics-std/statistics-std", "text": "标准差" },
      { "pagePath": "pages/user/user", "text": "我的" }
    ]
  }
}
```

## 🔐 服务器域名配置

在微信公众平台中配置允许的服务器域名：

1. 登录 [微信公众平台](https://mp.weixin.qq.com)
2. **开发** → **开发管理** → **开发设置**
3. 找到 **服务器域名** 部分
4. 点击 **修改**
5. 添加你的后台服务域名（如 `http://10.0.0.108`）

**重要**：
- 生产环境必须使用 HTTPS
- HTTP 仅限开发调试
- 需要验证域名所有权

## 📱 常见问题

### Q: 哪里可以找到我的 AppID？

A: 在 [微信公众平台](https://mp.weixin.qq.com) 的开发设置中。

### Q: 我没有创建小程序账号怎么办？

A: 需要先在微信公众平台注册一个小程序：
1. 访问 https://mp.weixin.qq.com
2. 点击"立即注册"
3. 选择"小程序"类型
4. 按照步骤完成注册

### Q: 修改 AppID 后还是报错怎么办？

A: 尝试以下步骤：
1. 完全关闭微信开发者工具
2. 删除项目目录下的 `.miniprogram` 文件夹
3. 重新打开项目

### Q: 我可以使用测试 AppID 吗？

A: 可以，用于本地开发测试。但如果想上传到微信或在真机测试，必须使用真实的 AppID。

## 🚀 下一步

完成配置后：

1. ✅ 点击"编译"编译项目
2. ✅ 在开发者工具中预览
3. ✅ 在真机中扫码测试
4. ✅ 准备好后上传版本

## 📚 相关文档

- [微信小程序开发文档](https://developers.weixin.qq.com/miniprogram/)
- [项目配置参考](https://developers.weixin.qq.com/miniprogram/dev/devtools/projectconfig.html)
- [服务器域名配置](https://developers.weixin.qq.com/miniprogram/dev/framework/ability/network.html)

## 🔗 有用的链接

- **微信公众平台**：https://mp.weixin.qq.com
- **微信开发者工具下载**：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
- **开发文档**：https://developers.weixin.qq.com/miniprogram/dev/framework/

---

**提示**：不要将你的真实 AppID 提交到公开的 Git 仓库！可以在 `.gitignore` 中添加 `project.config.json` 来避免提交敏感信息。

最后更新：2024-11-28
